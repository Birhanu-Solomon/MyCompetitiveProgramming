class Solution {
    public int majorityElement(int[] nums) {
        
        // Sort the array in increasing order
        Arrays.sort(nums);

        //Our target element is found at n/2 index.
        int n = nums.length;
        return nums[n/2];

    }
}
